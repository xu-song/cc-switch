# CC-Switch 代理日志流向

```
┌──────────┐       ┌──────────┐       ┌──────────┐
│  CC CLI  │──────►│ CC-Switch│──────►│  上游     │
│ Claude   │   ①   │  (代理)  │   ②   │ OpenAI   │
│ Desktop  │◄──────│          │◄──────│ Gemini   │
│  (下游)   │   ④   │          │   ③   │ (上游)    │
└──────────┘       └──────────┘       └──────────┘
```


Upstream 和 Downstream 是相对于代理服务器而言的，不是绝对的。

## 四步数据流

| 步骤 | 方向 | 数据 | 格式 |
|:---:|:---|:---|:---|
| **①** | 客户端 → 代理 | 请求 | Anthropic（原始）|
| **②** | 代理 → 上游 | 请求 | OpenAI（转换后）|
| **③** | 上游 → 代理 | 响应 | OpenAI（原始）|
| **④** | 代理 → 客户端 | 响应 | Anthropic（转换后）|

## 两个记录视点（`full_log_upstream` / `full_log_client`）

完整日志按「视点」记录哪一侧的数据。两个视点是**独立的布尔开关**，可只开一个，也可同时开启；由全局配置 `full_log_upstream`（默认开启）与 `full_log_client`（默认关闭）分别控制。

> 两个视点可同时开启：届时一次请求会落**两条**记录，靠每条记录的 `perspective` 字段（`"upstream"` / `"client"`）区分。至少需保留一个视点开启。

### Upstream 视点（默认开启）

记录**代理 ↔ 上游**之间的真实数据，适用于排查代理到上游的交互问题。对应开关 `full_log_upstream`。

| 步骤 | 方向 | 是否记录 |
|:---:|:---|:---:|
| ① | 客户端 → 代理 | ❌ |
| **②** | 代理 → 上游 | **✅ 记为 request**（转换后）|
| **③** | 上游 → 代理 | **✅ 记为 response**（原始）|
| ④ | 代理 → 客户端 | ❌ |

### Client 视点（默认关闭）

记录**客户端视角**所见的完整对话，适用于复现客户端端看到的请求与响应。对应开关 `full_log_client`。

| 步骤 | 方向 | 是否记录 |
|:---:|:---|:---:|
| **①** | 客户端 → 代理 | **✅ 记为 request**（原始）|
| ② | 代理 → 上游 | ❌ |
| ③ | 上游 → 代理 | ❌ |
| **④** | 代理 → 客户端 | **✅ 记为 response**（转换后）|

## 小结

| 视点 | 开关 | request 来源 | response 来源 |
|:---|:---|:---|:---|
| **Upstream** | `full_log_upstream` | ② 转换后请求（发往上游）| ③ 上游原始响应 |
| **Client** | `full_log_client` | ① 客户端原始请求 | ④ 转换后响应（回给客户端）|

> 说明：在不涉及格式转换的直通（passthrough）链路上，①②、③④ 内容相同，两个视点记录结果一致；
> 仅在发生格式转换（如 Anthropic ↔ OpenAI / Responses）时两者才有区别。
> 视点与客户端协议格式（`client_format`，如 claude / codex / openai / gemini）是不同维度：视点描述「记录哪一跳的数据」，`client_format` 描述「客户端用哪种协议」。
